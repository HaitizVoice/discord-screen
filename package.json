{
  "name": "discord-screen",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "postinstall": "node -e \"console.log('\\n  Pronto. Agora rode:  npm run configurar\\n')\"",
    "configurar": "node scripts/configurar.mjs",
    "tunel": "node scripts/tunel.mjs",
    "dev": "concurrently -k -n site,servidor,tunel -c magenta,blue,yellow \"npm -w client run build -- --watch\" \"node --watch server/index.js\" \"node scripts/tunel.mjs\"",
    "start": "npm run build && npm -w server run start",
    "build": "npm -w client run build",
    "smoke": "node scripts/smoke.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
